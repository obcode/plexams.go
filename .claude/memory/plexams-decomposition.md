---
name: plexams-decomposition
description: "Marathon to decompose the plexams god-package into service packages; strategy = net-first, then peel concerns"
metadata:
  node_type: memory
  type: project
  originSessionId: 6285039b-3933-4bb1-a8f3-24a7355c4a1d
---

Goal: break the ~24k-LOC `plexams` god-package into service packages so email/pdf orchestration depends on small focused interfaces (see [[cli-to-gui-migration]]). Chosen approach: "erst Domäne zerlegen" before removing `plexams/email_*.go`.

Key finding (2026-07): **there is no clean leaf.** Every concern is coupled both via `*Plexams` AND woven across files. Even `anny` (smallest outbound: db+config) has ~12 inbound callers and its core functions are spilled across files: `annyBookedBySlot` (preplan_booking.go, feeds the generator), `ExahmRoomsFromAnnyBookings` (rooms.go), `maxNonAnnySebRoom` (preplan_overview.go). Output concerns (pdf/email) are the inverse: low inbound, ~30 outbound domain calls → big interfaces.

Strategy: per-concern slice = (1) characterization tests around the area, (2) untangle spilled funcs + extract to package with a small `deps` interface `*Plexams` satisfies, (3) rewire callers to `p.<svc>.X`, (4) semilinear merge (`--no-ff` + push, see [[git-workflow]]). Order: shared booking/room logic first (riskiest, feeds generator), output layers (pdf/email) last.

**How to apply:** Status — Slice 0 DONE (19151a2): char. net for anny/room booking logic. Slice 1 DONE: `plexams/anny` extracted. Also: renamed the anny-fed `BookedEntry`→`AnnyRoomBooking` + dropped dead booked code; a dead-code sweep (355 lines: ChangeRoom, GetExamsForStudent, InvigilationTodos, commented blocks). Slice 2 DONE (55c0d56): `plexams/primuss` — the Primuss XLSX import subsystem (tiny DB iface RawCollection/ReplaceRawCollection, pure parsing; delegators ImportPrimussZip/Dir; orchestration HTTPUploadPrimussZip/affectedZpaAncodes + the thin query/fixData db-passthroughs stayed in plexams).

Slice 3 DONE (1ecae53): `plexams/planstate` — the generic condition/event Petri-net ENGINE (Machine over a 2-method DB iface). Solved the cond*-constant churn by **injection**: the concrete net (cond* keys + phase/cond defs) stays declared in plexams and is passed to `planstate.New(db, phases, conds)`; delegators keep identical signatures → zero call-site churn. Engine now unit-testable with a fake DB (no mongo).

Slice 4 DONE (4babdf7): `plexams/invigcalc` — the pure fair invigilation-share math (FairTargets: factor-weighted share + largest-remainder rounding; Todos) moved out of the 860-line invigilators.go with its table test. I/O-free, model-only.
Slice 5 DONE (50f415c): `plexams/preplancalc` — pure SEB/EXaHM room-capacity math (RoomCapacity, TotalSeats, RoomsForKind, KindNeed, RoomsToBook, ApplyBooking, ProgramConflicts, NormRoomName). Kept a lean cut: slotBooking/slotBlockDuration/validatePreplan (uses pluralN) stayed in plexams; preplanRoomCapacities builds preplancalc.RoomCapacity; annyBookedBySlot uses preplancalc.NormRoomName. Added tests for previously-untested capacity math.
Slice 6 DONE (3f7d51c): `plexams/conflictcalc` — pure schedule-conflict math out of exam_conflict.go: NormPair (canonical ancode-pair key), ProximityRank (worst→mildest ordering — collapsed the duplicated proxRank/proximityRank into one), SlotProximity (rank+label of two placed slots), DiffAgainstSaved (tag generated conflicts new/worse/better/unchanged, return resolved). The DB-coupled aggregation `conflictsFromSlots` (needs StudentRegs/decisions/canShare/sameSlotGroups/examInfoMap + shared helpers semesterOf/repeatForStudent) stayed in plexams and calls the pure pkg. Added table tests for the previously-untested correctness-critical comparison logic. Note: semesterOf/repeatForStudent/minGroupSemester/firstProgram/locationOf are shared with examplan_build.go — a future examplan_build_helper slice could pull those.
Slice 7 DONE (fe20f01): `plexams/roomcalc` — pure room-seat & constraint math out of roomsPrepare.go: SatisfiesConstraints (the EXaHM/SEB/Lab/socket/allowedRooms feature-matching predicate — correctness-critical, EXaHM⊇SEB compatibility), FreeSeatsBuffer (max(FreeSeatsMin=2, ceil(FreeSeatsPercent=5 %))), ExamFreeSeats (free seats across an exam's rooms; NTA-alone excluded, reserve counts as free). Constants moved too. The stateful allocation (takeReserveRoom/findRoom/findSmallestRoom/addReserveBuffer, all mutate cfg.availableRooms) + DB access stayed in plexams and call the pure pkg; validate_rooms.go shares FreeSeatsBuffer. Added table tests. The rest of roomsPrepare.go (~940 LOC) is stateful cfg-driven allocation — NOT a pure-core candidate; a full rooms domain slice would need the prepareRoomsCfg state machine untangled.
Slice 8 DONE (15122fa): `plexams/repeatcalc` — the study-group-semester repeat heuristic shared between examplan_build.go and exam_conflict.go: SemesterOf (first digit run of a group code, e.g. IF4B→4), MinGroupSemester (smallest), RepeatForStudent (repeater flag OR student semester > exam semester). Drove conflict auto-accept + generation down-weighting; both files now call the pure pkg. Added table tests. Remaining shared-but-trivial helpers left in plexams (not "math", low value): locationOf/firstProgram (3-line model accessors), intersectSlots/intersectAllowed/maxInt (examplan_build-local slot-set math).
Slice 9 DONE (16e9a61): `plexams/zpaimport` — the pure ZPA-import decision logic. **Finding: unlike primuss, the zpa import (zpa_import.go) is mostly orchestration glue over existing *Plexams/db methods (p.GetTeachers/GetZPAExams/GetSupervisorRequirements/GetInvigilators/markCondition/logSync/PrepareInvigilationTodos), NOT a separable subsystem** — so this became a pure-core (d) extract, not a full domain move. Extracted: DiffChanges (the generic old-vs-new import diff — now RETURNS the SyncLogEntry + report lines instead of writing to a Reporter → pure/testable; reportImportChanges stayed as a thin delegator that streams the lines via reporter.Printf("%s", line) → zero call-site churn at the 3 Import* funcs), ExamShouldBePlanned (classify for auto-preselect), Preselect (new to-plan/not-to-plan sets for undecided exams). DB/client/reporter/condition I/O stayed in plexams. Pkg named `zpaimport` to dodge the top-level `zpa` clash. Added table tests incl. exact report-line ordering. NOTE: zpa_get.go/zpa.go/zpa_post.go remain — thin db/client passthroughs + UploadPlan/PostStudentRegs orchestration (couples to plan/invig); no clean pure core there.
Slice 10 DONE (5683ba1): `plexams/examplan` gained pure builders — extracted the examplan-PURE assembly helpers from the ~500-line buildExamPlanProblem into the existing examplan solver pkg (it imports NO model, so only examplan-typed logic could move without adding coupling): AttractPairs(units, smallExamThreshold) — parallel-section (same module+program, different examer) + small-same-examer clustering; IntersectAllowed/IntersectSlots — allowed-slot index-set math (same-slot group Allowed + MUC.DAI restriction). buildExamPlanProblem calls examplan.X now; DB fetches + model-coupled assembly stay in plexams. Added table tests.
**Full buildExamPlanProblem extraction is NOT worth it / blocked:** p.AllowedSlots(ctx, ancode) is called INSIDE the movable/fixed assembly loop (can't pre-fetch only-movable without duplicating the classification), and a data-in Build() would need ~12 pre-fetched inputs; high risk w/ no mongo integration harness. The remaining big pieces there are model-coupled (union-find over constraints.SameSlot, unit building, conflict-pair weighting over studentsRaw+decisions, gap-forbidden over NTA durations) → would need a model-importing new pkg, not examplan.
Slice 11 DONE (a75b5db): `plexams/invigplan` gained Problem.DeviationOutliers — same slice-10 pattern (move an owning-pkg-pure computation into its already-extracted pkg). Extracted the fair-share deviation ranking from printDeviationOutliers (invigilation_generate.go) into invigplan/stats.go next to MinuteSummary/DistributionOf: ranks invigilators by relative deviation |dev|/max(target,ToleranceMin), returns Outlier{ID,Doing,Target,Open,Percent}, excludes on-target, topN<=0=all. plexams keeps reporter output + model.InvigilatorOutlier mapping (thin formatter). Table tests added.
Technique inventory now: (a) extract logic + small deps iface + facade delegators; (b) inject policy, move mechanism; (c) leave thin db-passthroughs; (d) extract pure computational cores; (d2, slices 10/11/12) move an ALREADY-separate pkg's own-typed pure computation from a plexams builder/reporter INTO that pkg (examplan.AttractPairs, invigplan.DeviationOutliers, invigplan Distribution.String/SortedCounts + Result.SortedCosts) — safe, no new coupling since the target pkg imports no model.
Slice 12 DONE (7ea8d31): moved the invigplan-typed formatting/aggregation out of invigilation_generate.go into invigplan/stats.go — Distribution.SortedCounts + Distribution.String (histogram "0:4 1:48 2:17"; killed plexams distributionString; fairnessModel reuses SortedCounts), Result.SortedCosts + CostItem (positive soft-cost breakdown desc; replaced inline kv sort). plexams keeps reporter/model mapping. Tests added.
d2 is now largely mined out. Remaining invigilation-report bits (colorCount/reportLabel/reportInvigilationReport shell) are aurora/reporter-bound, not pure. examplan diagnostics.go already lives in-pkg. Next d2 would be marginal.
Slice 13 DONE (f074564): **started the OUTPUT-CONCERN peel (the original marathon goal) — new `plexams/pdfgen` pkg.** Technique (e): split "gather data (stays in plexams)" from "render (moves to pkg)". Moved pdf.go's 3 reports' maroto rendering out: ExamsToPlan/SameModulNames/Constraints render funcs take plain data (semesterFull, jiraURL strings + exam slices) + a shared footer/centeredRow helper; pure tested row-builders (ExamsToPlanRows/SameModulNamesRows/ConstraintsRows + SameSlotAncodes) do the grouping/sort/per-constraint formatting. plexams pdf.go now only fetches + calls pdfgen + writes file (dropped color/consts/props/time/sort imports). KEY trick for the constraints report: its layout had a per-row p.GetZpaExamByAncode I/O call — plexams now pre-resolves those via pdfgen.SameSlotAncodes into a map[int]*ZPAExam passed to the renderer, keeping render I/O-free. Behavior byte-identical (incl. the latent nil-deref if GetZpaExamByAncode errors — preserved on purpose).
Slice 14 DONE (0526f69): pdf peel continued — pdfDraft.go 483→224 LOC. Added to pdfgen: DraftDoc(landscape,title,planerName,planerEmail) shared draft shell (killed the ~50-line footer/title/planer/"zur Abstimmung" boilerplate ×4 in DraftMucDai/Fk08/Fk10/ExahmPDF), ProgramRows(exams,program,slotTime func) + ProgramTable (per-program table; pure row logic tested with a fake slotTime incl. the zero-reg-section break OUTER quirk), FormatTermin (Mo. 02.01.06 15:04 Uhr). plexams keeps fetch; tableForProgram is now a thin fetch+call. NOTE: the pkg-level `var r` date replacer stays in pdfDraft.go (still used by tableForExahm + pdfDraftSI.go) — pdfgen has its own dayReplacer/FormatTermin; dedupe when those move.
Slice 15 DONE (f1466bb): **pdf peel FINISHED** — the last three draft renderers moved into pdfgen. Generalized `DraftDoc(landscape,title,planerName,planerEmail,note)` with a `note` param so one shell serves both "--- zur Abstimmung ---" (MucDai/Fk08/Fk10/EXaHM) and "--- ENTWURF ---" (FS/SI) — killed the duplicated footer/title/planer boilerplate in pdfDraftFS.go+pdfDraftSI.go. Added pure tested builders + thin renderers: ExamRows/ExamTable (SI + LBA-repeater drafts, sort-by-ancode) and ExahmRows/ExahmTable/ExahmHeading (EXaHM/SEB draft; sortByDate copies before sorting = no input mutation). tableForExahm pre-resolves PrePlannedRoomsForExam into a map[int][]string (only for exams w/o PlannedRooms) so render is I/O-free — same trick as the constraints report. Termin formatting now goes through pdfgen.FormatTermin; **the pkg-level `var r` date replacer is gone from pdfDraft.go** (the `r` in exam.go:296 is a shadowed local in foldUmlauts, unrelated). pdfDraftSI.go dropped its pdf import entirely. Byte-identical; API/CLI/GUI unchanged. pdf.go/pdfDraft*.go are now thin fetch+call; the whole pdf output concern renders from plexams/pdfgen.
Slice 16 DONE (2783256): **started the email peel.** Finding: the RENDER layer is already fully in plexams/email (Renderer + templates + markdown, see [[email-markdown-templates]]); every email builder calls p.mailRenderer().Render(). So the remaining email "mass" is data-GATHERING (I/O-coupled) + view-model shaping. First slice = the cross-cutting foundation: the German date/time formatter. The weekdayShortDE map lived in email_room_requests.go but the "Mo, 06.07.2026" pattern was inlined in 7 builders (kdp/lbaba/nta/published_rooms/room_requests/rooms_secretariat). Moved to email pkg as tested pure helpers WeekdayDE("Mo")/DateDE("Mo, 06.07.2026")/TimeDE("08:30"); rewired all sites; dropped the plexams map. Byte-identical (golden tests unchanged).
Slice 17 DONE (c799c4e): **first per-email (e)-split — KDP EXaHM/SEB room overview.** email_kdp.go 323→87 LOC. Moved the ~200-line per-slot/room/exam seat aggregation + view-model to email/kdp.go: pure `BuildKdp(plannedExams, slotTime) ([]*KdpSlot,[]CsvKdpRoom)` (types KdpEmail/KdpSlot/KdpRoom/KdpExamInRoom/CsvKdpRoom + kdpDetail/intsJoin moved; the EXaHM/SEB filter became exported email.IsExahmSeb). KEY trick: the GetTeacher cc-lookups sat INSIDE the aggregation loop → pulled them OUT into a plexams I/O-only helper kdpExamerCC(ctx, plannedExams) (2 passes over the exam list, negligible), so BuildKdp is pure. plexams SendEmailKdpExahm now: fetch PlannedExams → BuildKdp → wrap KdpEmail{semester,planer,slots} → kdpExamerCC → csv/render/send. Aggregation was untested before; added table tests (normal/NTA/reserve seat classes, ONLINE-room skip, ancode/room/slot ordering, CSV rows). Golden test retargeted to email.Kdp* types, output byte-identical.
Email-peel pattern established: (1) the builder's I/O (GetTeacher/PlannedRoomForStudent/GetInvigilator*) stays in plexams — often pull it OUT of the shaping loop into a pre-resolve/helper; (2) the pure view-model shaping + its types move to plexams/email as BuildXxx; (3) plexams keeps fetch→BuildXxx→render→send.
Slice 18 DONE (113d588): **exam-planning recipient grouping.** The ExamPlanningMailRecipients *query resolver* did ~80 lines of pure grouping/sorting inline between its 3 fetches (ZpaExamsToPlanWithConstraints/GetTeachers/GetZPAExams). Moved to pure `email.BuildExamPlanningRecipients(withConstraints, teachers, allExams) → []*model.ExamPlanningMailRecipient` (the fk07="FK07" faculty-marker const moved with it). plexams method now just fetches the 3 inputs + delegates → resolver signature unchanged (zero GraphQL/GUI churn). Grouping (planned-by-me per examer, exam sort by ancode, external-examer stub, fk07NoExams category, withExams-first ordering) was untested → table tests added. constraintsText + SendExamPlanningInfoMails + examPlanningInfoMailData stay in plexams (constraintsText is a shared emailFuncs template func, also used by pdf/statistics/preplan — do NOT move it; would cause wrong-direction coupling).
Slice 19 DONE (59d238c): **published-rooms view-model.** email_published_rooms.go 260→~95 LOC. Moved types PublishedRoomsEmail/PublishedRoomsExam/PublishedRoomsRoom/PublishedRoomsShared + intricate pure builders roomAllocations (seat blocks: non-NTA first, then by duration), ntaNote, BuildPublishedRoomsExam (per-room co-usage grouping, first-seen room order) + SortPublishedRoomsExams (start field unexported → sort helper instead of exposing it) into email/published_rooms.go. BuildPublishedRoomsExam takes the pre-fetched examsInSlot map + examerShort closure + slotTime func → all I/O (ExamsInSlot/GetTeacher caches) stays in plexams' send loop unchanged. **roomNTA entanglement resolved by DUPLICATION**: the 8-line pure roomNTA is copied as an unexported email helper (ics_invigilator.go keeps its own) rather than coupling ics→email — the right call for a trivial pure model accessor shared by two output concerns. Shaping was untested → table tests (seat-block ordering, NTA note w/ eigener Raum, co-usage grouping, nil cases). Golden test retargeted to email.PublishedRooms*; byte-identical.
Slice 20 DONE (9fc5c34): **assembled/unplanned attachment builders.** The "Anmeldungen-*.csv" student-reg CSV (Mtknr as an Excel formula ="000123" to keep leading zeros) was built VERBATIM in two places (sendAssembledExamMailToTeacher + SendUnplannedExamMail). Extracted to email pkg as pure: StudentRegsCSV(regs []*model.EnhancedStudentReg) []byte + StudentRegsOfPrimussExams (flatten an AssembledExam's sections) + RenderAssembledMarkdown(data any) (the standalone .md attachment, was rendering the embedded tmpl inline in plexams via txttmpl → moved, killing the bytes/text-template imports there). Both send funcs call the shared builders now; dedup + tested (CSV rows/flatten/markdown — golden test only covers the body, not attachments). d-style pure-core extraction.
Slice 21 DONE (aa08282): **LBA-repeater view-model.** Moved LbaRepeaterEmail/LbaRepeaterExam/LbaPerson/LbaProgram + pure BuildLbaRepeaterExams(plannedExams, slotTime, examer, invigilatorForRoom) into email/lbaba.go (non-prof filter, per-room invigilator dedup with ONLINE skip, programs count>0 sorted, chronological order). The two interleaved I/O calls (GetTeacher for IsProf/email, GetInvigilatorForRoom per room) became **injected closures returning nil on error/skip** — same closure-injection pattern as published_rooms' examerShort. plexams buildLbaRepeaterExams = fetch + 2 closures + delegate. Untested shaping → table test (all 4 filter branches, unplanned "noch nicht geplant", invig dedup, program sort, ordering). Golden retargeted to email.Lba*; byte-identical.
Slice 22 DONE (6bbe228): **shared room-occupancy view-model** (room_requests + rooms_secretariat both built the same room→days→times model). Moved RoomRequestEmailRoom/Day/Time + RoomRequestEmail + SecretariatRoomsEmail + lastDay + mergeRoomIntervals/roomInterval into email/room_occupancy.go, plus two pure builders: BuildRoomRequestRooms(requests) (filter active, sort by room/start, group) and BuildSecretariatRooms(plannedRooms, roomInfo, slotTime) (filter to real active non-request rooms, collect intervals, merge overlapping/touching, group). Both send funcs = fetch + delegate. Untested grouping+merge → table tests (merge overlap/touch/disjoint/unsorted, request grouping, secretariat filtering). Golden retargeted to email.RoomRequest*; byte-identical.
Email peel status: kdp 323→87, published_rooms 260→95, lbaba 176→~90, room_requests+rooms_secretariat deduped into one shared model, exam_planning slim, assembled deduped. The email pkg now holds format/kdp/exam_planning/published_rooms/attachments/lbaba/room_occupancy builders + renderer/templates. **The high-value email pure cores are now mined out.** Remaining email files are thin fetch-loops with little separable pure shaping: email_nta.go (312 — per-student fetch loops interleaved w/ PlannedRoomForStudent/GetInvigilatorInSlot; closure-injection possible but low payoff), email_published.go (267), email_cover_pages.go. Thin/orchestration-only (exahm/invigilations*/constraints/draft) need no split. email_attachments.go = http handlers + db passthroughs, not worth a slice. **Email peel DONE — switched concern to the rooms area.** Techniques toolkit: closure-injection (examer/invigilator/examsInSlot funcs returning nil-on-skip) for I/O interleaved in a shaping loop; duplication-of-trivial-pure-helper (roomNTA) when shared across two output concerns; move attachment/CSV/markdown content-gen into email.
Slice 23 DONE (1fd79f1): **Anny room-booking type + pure math → anny pkg.** Moved AnnyRoomBooking→anny.RoomBooking, mergeAnnyRoomBookings→anny.MergeRoomBookings (sort by normRoom/approval/time + merge adjacent/overlapping same-room+approval windows), roomBookedDuringExamTime→anny.RoomBookedDuringExamTime (inclusive 90-min cover, used by validate.go), and extracted the EXaHM slot-coverage predicate → anny.CoversSlot (exclusive, start+89min; used in rooms_for_slots.go restrictedSlotsForEXaHMRooms — reporter lines preserved, only the predicate swapped). rooms.go/validate.go/rooms_for_slots.go call anny now. **The two coverage predicates are DELIBERATELY distinct (inclusive+90 vs exclusive+89 — different rules); did NOT unify.** normRoom keeps dots, only strips spaces+uppercases. Migrated the merge characterization tests (from anny_pure_test.go + the redundant rooms_test.go, deleted) into anny/booking_test.go + added name-normalization & both-predicate cases. Removed the pointless first copy-loop in the merge. anny_pure_test.go keeps only TestSlotBlockDuration (slotBlockDuration stays in plexams — uses model.Starttime).
**Remaining rooms work is the hard core:** roomsPrepare.go (~917 LOC) is a stateful prepareRoomsCfg machine — findRoom/findSmallestRoom/takeReserveRoom/addPlannedRoom/setPrePlannedRooms/assignPrePlannedRooms all MUTATE cfg (availableRooms slice removal, seatsTakenMap, plannedRoomsWithFreeSeats) + reporter side-effects; roomcalc already holds the pure predicates (SatisfiesConstraints/FreeSeatsBuffer/ExamFreeSeats, slice 7). No clean pure core left there without an integration-test harness (none exists; SlotNumber has unexported fields blocking cross-pkg moves). validate*.go = reporter(v.errorf)-coupled rules over pkg-global knownConflictsCount; not cleanly pure. Slice 24 DONE (bbb3254): **two more pure cores chipped out of roomsPrepare into roomcalc** (no harness needed — the state machine stays). roomcalc.SortPrePlannedRooms(rooms, roomInfo) = an exam's pre-planned rooms in fill order (NTA-rooms first, non-reserve by descending seats, reserve last; kept sort.Slice NOT stabilized to preserve byte-behavior — the comparator returns false for "equal" so stability was never guaranteed). roomcalc.ExamRegsAndNTAs(exam) = partition regs (NTAs → alone/normal rooms; normalRegs = non-NTA Mtknrs in section-then-reg order) — was inlined in mkExamsMap; caller still appends additionalSeats dummies + logs. Both untested → table tests. **The rooms pure-core well is now essentially dry** — remaining roomsPrepare funcs (findRoom/findSmallestRoom/takeReserveRoom/addPlannedRoom/setPrePlannedRooms/assignPrePlannedRooms/availableRoomsInSlot) all mutate cfg (availableRooms/seatsTakenMap/plannedRoomsWithFreeSeats) or do DB I/O + reporter — a full extract needs a mongotest characterization harness (harness EXISTS: internal/mongotest.NewDB(t), used by 3 integration tests; but seeding PrepareRoomForExams needs rooms+blockedRooms+roomsForSlots+assembledExams+planEntries+regs+NTAs+prePlannedRooms — heavy fixture work, deferred).
Slice 25 DONE (0c4abeb): **new plexams/csvgen pkg — the CSV output concern peel** (parallel to pdfgen; found by sweeping untouched large files). csv.go's three exporters had the same shape as pdf/email (fetch→build rows→marshal→write). Extracted the two cleanly-pure ones: csvgen.ProgramRows(exams, program, slotTime) (per-program rows: one per planned room + NTA/reserve/student comment cell via roomComment, placeholder when no rooms, keyed+sorted by Primuss ancode, zero-reg-section & no-match exams dropped) and csvgen.ExahmRows(exams, slotTime). plexams CsvForProgram/CsvForEXaHM now fetch+delegate+marshal+write. Dropped a dead commented line + the "primuss ancode not found" diagnostic log (exam was already silently dropped — not an artifact). CsvForLBARepeater STAYS in plexams (per-room GetInvigilatorForRoom I/O with abort-on-error dominates; extracting would need error-returning closures for marginal gain). Untested → table tests. Byte-identical (note: set.ToSlice room dedup was already non-deterministic; tests use single rooms).
Slice 26 DONE (3933631): **new plexams/mucdai pkg — the MUC.DAI CSV import parser** (mirrors primuss slice 2). Moved parseMucDaiCSV→mucdai.ParseCSV + the column map + normalizeHeader/latin1ToUTF8/detectDelimiter (encoding ISO-8859-1/BOM + delimiter ,/;/tab auto-detect, mongoimport type-suffix-tolerant headers, grouped by program → map[string][]*db.MucDaiExam). plexams ImportMucDaiExams calls mucdai.ParseCSV; the DB orchestration (replace collections, generate/prune external exams, backfill faculties, relink) stays in plexams. Test file moved into the pkg. GOTCHA: the BOM strip literal must be the `"﻿"` ESCAPE, not a literal BOM byte — Go rejects a mid-file BOM (`invalid BOM in the middle of the file`); the exact-match Edit also fails on the BOM byte, use sed/perl by line range. Found by sweeping untouched large files — same technique as csvgen (slice 25).
**Marathon status (26 slices): the god-package is now a thin-ish orchestration/IO shell over 16 service packages** (anny, primuss, mucdai, planstate, invigcalc, preplancalc, conflictcalc, roomcalc, repeatcalc, zpaimport, examplan, invigplan, pdfgen, csvgen, email). FALSE-START (do NOT retry): moving mucdai_links.go's autoMucDaiLink (pure, already tested in mucdai_links_test.go) into the mucdai pkg is BLOCKED — it takes map[primussKey]... params, and `primussKey{program,ancode}` is a plexams-WIDE shared type (used across exam.go ×6, primuss.go, mucdai.go, not just the mucdai files). Moving primussKey→mucdai.PrimussKey would force unrelated exam/primuss code to import mucdai — semantically wrong. Decoupling AutoLink from the key type (pass looked-up []int/*int instead of maps) is possible but low-ROI since it's already pure+tested. Left in plexams.
Slice 27 DONE (13c7bf2): **rooms characterization HARNESS built** (the safety net for extracting the roomsPrepare state machine). roomsPrepare_integration_test.go: mongotest-backed (PLEXAMS_TEST_MONGO_URI or testcontainers, SKIPS otherwise → `go test ./...` stays green). KEY technique that makes it tractable: **build the `*prepareRoomsCfg` BY HAND in the test** (roomInfo/prePlannedRooms/additionalSeats/roomsForSlots/blockedRooms/exactSeatRooms/slot) — this BYPASSES the DB-heavy prepareRoomsCfg builder (computeRoomsForSlots→anny/reservations). Then only 3 collections need seeding: rooms (AddRoom), plan entries (AddExamToSlot), assembled exams (CacheAssembledExams) — that's all db.ExamsInSlot + availableRoomsInSlot(RoomByName) read. Use newDiscardReporter(). SlotNumber{day,slot} constructible in-package (test is package plexams). Helpers: roomsTestPlexams/simpleExam/seedSlot/slotCfg. **5 scenarios (1b04bab)** pin the major distinct code paths: (a) happy path — 2 exams incl. NTA-normal + NTA-alone, all 32 seats placed, 1 HandicapRoomAlone + 1 handicap room; (b) overflow → unplaced remainder; (c) reserve-buffer (packed below buffer → +1 reserve); (d) EXaHM-constraint (EXaHM exam only in an EXaHM room, never a bigger plain room — SatisfiesConstraints/findRoom); (e) pre-planned room (filled first, marked PrePlanned, students not re-placed — setPrePlannedRooms/assignPrePlannedRooms). Covers findRoom, findRoomWithFreeSeats/constraint filtering, setPrePlannedRooms, unplaced, addReserveBuffer, both NTA passes, findSmallestRoom, mkExamsMap. **To run locally w/o Docker: see [[mongotest-without-docker]] (standalone mongod on :27099).**
**EXTRACTION assessment (the remaining big step):** it is a ~600-LOC move of the whole per-slot machine (prepareRoomsForExamsInSlot + mkExamsMap + availableRoomsInSlot + setPrePlannedRooms/assignPrePlannedRooms + findRoom/findSmallestRoom/takeReserveRoom/findRoomWithFreeSeats + addPlannedRoom + addReserveBuffer + the prepareRoomsCfg/plannedRoomsWithFreeSeats structs) into a `rooms` pkg. It is mostly a MECHANICAL verbatim move (logic unchanged → harness catches covered paths, build catches type errors), the real risks being: (1) map key SlotNumber→a rooms-local SlotKey{Day,Slot} conversion at the builder boundary (do NOT move SlotNumber itself — it's plexams-wide, used in exam.go/primuss.go); (2) a minimal rooms.Reporter iface (Step/Println/Warnf only) that *Plexams satisfies structurally; (3) a rooms.DB iface (ExamsInSlot + RoomByName) that *db.DB satisfies; (4) exporting the Cfg fields (or a constructor) so the plexams prepareRoomsCfg builder can populate it. Uncovered-by-harness sub-paths to eyeball during the move: shared pre-planned pass-2, blocked rooms, exact-seat rooms, findRoomWithFreeSeats duration matching, multi-slot roomsNotUsableInSlot carryover (that carryover lives in the PrepareRoomForExams outer loop, which stays in plexams). This is a big, error-prone single edit — best as its own focused session; the harness is the milestone that de-risks it.
**RECOMMENDATION — decomposition is substantially COMPLETE (27 slices, 16 service pkgs + rooms harness).** The remaining pure cores are all either (a) already pure+tested where they sit (autoMucDaiLink, best, examPair) so a move is cosmetic-only, (b) small helpers needing a new package for ~2 funcs (enhancePrimussExam/primussAncodesToZpaAncodes — assembly, no natural home), or (c) the genuinely-hard stateful rooms machine (roomsPrepare cfg) / reporter-coupled validate*.go that need the mongotest characterization-harness investment first. Net: further extraction is diminishing-returns cosmetics OR a big harness project. Treat the marathon as done unless a subsystem needs active change (then build its test harness as part of that work). The plexams package is now a thin CLI/GraphQL/DB-orchestration shell over the 16 pkgs — that is a healthy end-state, not residue to force further. Pure cores are mined out across pdf/email/rooms/conflict/invig/preplan. **Remaining residue is inherently stateful (roomsPrepare cfg machine) or reporter/IO-coupled (validate*.go) — needs the mongotest characterization-harness investment before further safe extraction, or is genuinely irreducible CLI/GraphQL glue.** Recommend: treat the decomposition as substantially COMPLETE; do the rooms-machine harness only if that subsystem needs active change.
Pure-core (d/d2) candidates now largely exhausted (anny, primuss, planstate, invigcalc, preplancalc, conflictcalc, roomcalc, repeatcalc, zpaimport, examplan-builders, invigplan-outliers). Remaining decomposition work is genuinely harder and needs a different technique: untangle a stateful state-machine (roomsPrepare prepareRoomsCfg), extract validation cores (validate*.go — but coupled to pkg-global knownConflictsCount + aurora + unexported types), or peel an output concern (pdf ~1.2k LOC data→maroto→bytes is more self-contained than email; both need a defined interface). No more clean leaves.

Patterns proven: (a) extract the LOGIC to a pkg with a small deps interface + facade delegators = zero API/CLI/GUI change; (b) when many call-sites share constants/config, **inject the policy** and move only the mechanism = zero churn; (c) leave thin 1-line db-passthroughs in plexams; (d) **extract pure computational cores** (I/O-free math over model types) into small tested packages — safe, isolates correctness-critical logic. Good remaining (d) candidates: exam_conflict.go (conflict math), preplan validatePreplan/roomsToBook (seat math), rooms seat allocation. **anny was the only truly clean leaf; the rest need one of these techniques.** Remaining: zpa (zpa_post couples to plan/invig — extract the fetch/import like primuss, leave upload orchestration; note name clash with the existing top-level `zpa` client pkg → use a different pkg name), preplan, invigilation (solver already in invigplan; consider extracting a pure core like fairInvigilationTargets), rooms. One concern per branch.
